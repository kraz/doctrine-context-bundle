Doctrine Context Bundle
=======================

This bundle adds additional context to the Doctrine's migrations and some ORM commands so that working with more
than one entity manager or connection in a project gets easier.
